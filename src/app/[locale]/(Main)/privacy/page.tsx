import React from "react";
import PrivacyEn from "@/Library/_Pages/Policy/PrivacyEn";

const Page = () => {
	// const { locale } = params;

	// return locale === "en" ? <TermsEn /> : <TermsAr />;
	return <PrivacyEn />;
};

export default Page;
